let surName = "França"
console.log(surName.length)

/* We can use "New" for create new objects, whith Strings, Numbers, Booleans
New is a object, so he have methods and properties
*/

let name = new String("Matheus")

console.log(name)
// Now, name is a object, whith one propertie

let number = String(10)
console.log(typeof number)

// I was pass 10 per parameter, but, when we use String without new before, he it works how converter, i turned 10 into a String

let num = Number("10")
console.log(typeof num) // Number

// The same it works with Boolean