// IF E ELSE

let x = 3 
let y = 7

if (x = 3) { // Se o x for igual a 3
    y = 8 // Isso vai acontecer
}

// Podemos criar caminhos/opções com if e else

console.log(y)

let name = "Matheus"
let surName = "França"

if (name === "Matheus" && surName === "França") { // Se isso acontecer
    console.log("Bem-vindo Matheus França") // Faça isso
} else { // Se não
    console.log("Você não está cadastrado") // Faça isso
}


// Usando o Else if podemos cirar mais que 2 situações