// Declarar uma variável chamada myLet, sme valor
let myLet

// Após declarada, atribua o valor de 10 à variável 
myLet = 10

// Declare uma nova variável chamada soma, e adicione uma instrução somando 15 e 8
let soma = 5 + 8

// Atribua à variável "soma" todo o valor dela, somando 1, usando o operador de abreviar
console.log(++soma)

let soma = 14 * 3
console.log(soma)

// Qual o valor dela até aqui? 42

// Declare uma variável chamada "Sou ninja" e dê a ela um valor booleano - True

let souNinja = true

// Declare uma variável "comida" que recebe um array com os valores: "Arroz" , "Feijão" e "ovo"

let comida = ["Arroz" , "Feijão", "Ovo"]

// Imprima o valor de feijão que está no array de comida

console.log(comida[1])

// Digite um código que verifica se a variável "Soma" é igual a váriavel "myLet"

    // let soma = 1
    // let myLet = 2
console.log(soma === myLet)

// Digite um código que virifica se a variável "Soma" é maior ou menor que a outra

console.log(soma > myLet) // False

// Crie uma função chamada "divisão" que recebe como parâmetro 2 números e retorne o resultado da divisão

function divisão(num1, num2) {
    return num1 / num2
}

console.log(divisão(50, 2))