let object = { 
    propriedade: "valor",
    propriedade2: 10,
    propriedade3: true 
}

console.log(object)

// Objeto são blocos de códigos que serve para organizar os dados do seu código, e fazem referência à objetos da vida real

let person = {
    name: "Matheus", 
    age: 30,
    weight: 85,
    height: 1.83,
}

// Como acessar uma propriedade
console.log(person.name)

let car = {
    color: "red",
    motor: 2.0,
    doors: 4
}

console.log(car)

let exemple = function() {return "Let exemple"}
console.log(exemple())