// IndexOf - He check if exist this value in your array

let numbers = [1, 2, 3, 4, 5]
console.log(numbers.indexOf(3)    /* > -1*/    )

// It returns the index the element is at

// Or i can put > -1, for it check if my argument exists returning 'true' or ''false'

// Too exists 'lastIndexOf'


// isArray - Check if argument passed is an array