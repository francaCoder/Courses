"""
Make a program that has a function called higher(), that receive several parameters with whole numbers.

Your program need analyze all values and show the higher.
"""


def higher(* numbers):
    print(max(values))


values = [2, 9, 4, 5, 7, 1]
higher(values)
